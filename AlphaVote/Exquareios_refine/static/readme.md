# Refinement Bash Submission


 ### clone the project with `git clone`

 ### use `npm install` or `yarn add` to install dependencies

 ### use `npm start` or `yarn start` to start choice coin app

# DEMO

![visuals](https://github.com/exquareios/Voting/blob/main/AlphaVote/Exquareios_refine/static/image/gif.gif)
